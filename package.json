{
  "name": "evt-listener",
  "version": "1.0.0",
  "description": "EventListener, as an object.",
  "main": "index.js",
  "scripts": {
    "test": "./node_modules/.bin/mocha",
    "coverage": "./node_modules/.bin/istanbul cover ./node_modules/.bin/_mocha --report lcovonly -- -R spec && npm run coveralls",
    "coveralls": "cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "keywords": [
    "evt-listener",
    "events",
    "listener",
    "EventListener"
  ],
  "author": "Brian Mullan <bmullan91@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "mocha": "~1.21.4",
    "chai": "~1.9.2",
    "istanbul": "~0.3.2",
    "coveralls": "~2.11.2",
    "mocha-lcov-reporter": "0.0.1"
  }
}
